name: Weekly dashboard email (auto)

on:
  schedule:
    - cron: "0 9 * * 5"  # runs 09:00 UTC (≈10:00 CET) every Friday
  push:
    branches: [ main ]

jobs:
  call-reusable:
    uses: ./.github/workflows/_send-dashboard.yml
    secrets: inherit   # <<< add this line
